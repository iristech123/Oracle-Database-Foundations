在RDBMS（关系数据库管理系统）中，以下是正确的组件：

- **Column（列）**：表中的一列，用来存储特定类型的数据。
- **Data Type（数据类型）**：定义列中可以存储的数据的类型，比如数字、字符、日期等。
- **Database（数据库）**：存储数据的最高级容器，包含多个表。
- **Table（表）**：由行和列组成的结构，用来存储具体的数据。

“Result（结果）、Plan（计划）、Sponsor（赞助人）、Architect（架构师）”并不是RDBMS的组成部分，因此不包括在内。